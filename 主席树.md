不是很懂，只是简单了解一点，比较重要的一点是能快速求出区间第k小，全称是可持久化线段树，对于每次修改动态开点，都会保留原来的版本。板子还是很重要的，起码过一道模板题检验板子的正确性。

[题目1]()洛谷模板题。

[题目2](https://ac.nowcoder.com/acm/contest/72370/D)
```cpp
#include <bits/stdc++.h>
using namespace std;
#define int long long
#define pii pair<int,int>
#define inf 0x3f3f3f3f
#define mid (l+r)/2
const int N=1e6+5;
const int mod=998244353;
int cnt,a[N];
int L[N<<5],R[N<<5],sum[N<<5],T[N];
int build(int l,int r)
{
    int rt=++cnt;
    sum[rt]=0;
    if(l<r) 
    {
        L[rt]=build(l,mid);
        R[rt]=build(mid+1,r);
    }
    return rt;
}
int update(int pre,int l,int r,int x)
{
    int rt=++cnt;
    L[rt]=L[pre];
    R[rt]=R[pre];
    sum[rt]=sum[pre]+1;
    if(l<r)
    {
        if(x<=mid) L[rt]=update(L[pre],l,mid,x);
        else R[rt]=update(R[pre],mid+1,r,x);
    }
    return rt;
}
int query(int u,int v,int l,int r,int k)
{
    if(l>=r) return l;
    int x=sum[L[v]]-sum[L[u]];
    if(x>=k) return query(L[u],L[v],l,mid,k);
    else return query(R[u],R[v],mid+1,r,k-x);
}
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    int n,q;cin>>n>>q;
    for(int i=1;i<=n;i++) 
    {
        cin>>a[i];
        a[i]++;
        a[i+n]=a[i];
    }
    T[0]=build(1,n);
    for(int i=1;i<=2*n;i++) T[i]=update(T[i-1],1,n,a[i]);
    while(q--)
    {
        int l,r,k;cin>>l>>r>>k;
        if(k>n-(r-l+1)) cout<<k+r-l<<"\n";
        else 
        {
            int t=query(T[r],T[n+l-1],1,n,k);
            cout<<t-1<<"\n";
        }
    }
    return 0;
}
```